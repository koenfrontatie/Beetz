using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SequencerContextMenu : MonoBehaviour
{
    [SerializeField] private RectTransform _dragger, _copy;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
